const n="download_file",a="Download External URL",s="Downloads any file, backup, data sheet, image, or ZIP archive from a public URL on the internet and saves it safely in the local user_data sandbox environment.",r="1.0.0",i="TOOL";const l={type:"object",properties:{url:{type:"string",description:"The precise public HTTP or HTTPS web URL of the file to download."},filename:{type:"string",description:"The target path to save the file in the workspace sandbox. You MUST specify the full path starting with 'user_data/' (e.g. 'user_data/spreadsheet.csv') or absolute '/app/user_data/' path. If left empty, Yui will intelligently guess the filename."}},required:["url"]},c={id:n,name:a,description:s,version:r,type:i,order:204,parameters:l};var d={};const f={metadata:c,execute:async e=>{try{const o=typeof window>"u"?`http://127.0.0.1:${d.PORT||"3000"}`:`${window.location.origin}`;return(await fetch(`${o}/api/tools/files/download`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url:e.url,filename:e.filename})})).json()}catch(t){return{success:!1,error:t.message}}}};export{f as DownloadFileTool};
