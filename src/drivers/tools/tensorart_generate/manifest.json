{
  "id": "generate_image",
  "name": "Generate Image",
  "description": "Generate high-quality visuals and anime art using TensorArt API's txt2img diffusion stages.",
  "version": "1.0.0",
  "type": "TOOL",
  "order": 5,
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "Operation mode: 'generate' (default), 'list_tools', or 'upload_file'.",
        "default": "generate"
      },
      "prompt": {
        "type": "string",
        "description": "Highly detailed vivid description of the scene or illustration to generate (e.g. 'anime girl under falling blossom petals, smiling')."
      },
      "toolName": {
        "type": "string",
        "description": "TensorArt tool/model name (default: anime_lab_wai_illustrious). Use list_tools to discover available tools.",
        "default": "anime_lab_wai_illustrious"
      },
      "width": {
        "type": "integer",
        "description": "Width of the generated visual in pixels (default: 512).",
        "default": 512
      },
      "height": {
        "type": "integer",
        "description": "Height of the generated visual in pixels (default: 512).",
        "default": 512
      },
      "inputs": {
        "type": "array",
        "description": "Full inputs array for the TensorArt tool. Each element: {type: 'STRING'|'INTEGER'|'FILE'|..., value: ...}. Overrides prompt/width/height when provided.",
        "items": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": ["STRING", "INTEGER", "NUMBER", "BOOLEAN", "ARRAY", "OBJECT", "FILE"]
            },
            "value": {}
          }
        }
      },
      "filePath": {
        "type": "string",
        "description": "Local file path to upload (required for action: 'upload_file')."
      },
      "timeoutMs": {
        "type": "integer",
        "description": "Override default timeout for this generation call (ms).",
        "default": 120000
      },
      "pollIntervalMs": {
        "type": "integer",
        "description": "Interval between status polling checks (ms).",
        "default": 3000
      },
      "retryLimit": {
        "type": "integer",
        "description": "Max retries on transient failure.",
        "default": 0
      }
    },
    "required": ["prompt"]
  }
}
