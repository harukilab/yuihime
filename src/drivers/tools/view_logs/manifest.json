{
  "id": "view_logs",
  "name": "View Logs",
  "description": "View Yuihime's system logs, audit trails, and LLM input/output details to diagnose system states, execution errors, or API issues.",
  "version": "1.0.0",
  "type": "TOOL",
  "order": 110,
  "parameters": {
    "type": "object",
    "properties": {
      "logType": {
        "type": "string",
        "description": "Type of logs to retrieve: 'audit' (tool execution & structure compliance audit logs), 'llm' (direct cognitive LLM language prompts & replies), or 'all' (unified audit + llm logs merged by timestamp).",
        "enum": ["audit", "llm", "all"],
        "default": "audit"
      },
      "limit": {
        "type": "number",
        "description": "Maximum number of recent log lines to retrieve (default is 10, max 100).",
        "default": 10
      },
      "offset": {
        "type": "number",
        "description": "Number of log lines to skip before collecting the page (default 0). Use together with 'limit' to paginate.",
        "default": 0
      },
      "filter": {
        "type": "string",
        "description": "Optional search keyword or tool name to filter logs (e.g., 'tensorart', 'error')."
      }
    },
    "required": []
  }
}
