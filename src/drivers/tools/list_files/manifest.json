{
  "id": "list_files",
  "name": "List Files",
  "description": "List all files available in user_data.",
  "version": "1.0.0",
  "type": "TOOL",
  "order": 103,
  "parameters": {
    "type": "object",
    "properties": {
      "limit": { "type": "number", "description": "Maximum number of file entries to return (default: all)." },
      "offset": { "type": "number", "description": "Number of file entries to skip before collecting the page (default 0)." }
    },
    "required": []
  }
}
