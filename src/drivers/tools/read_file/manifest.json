{
  "id": "read_file",
  "name": "Read File",
  "description": "Read content from a file in user_data.",
  "version": "1.0.0",
  "type": "TOOL",
  "order": 102,
  "parameters": {
    "type": "object",
    "properties": {
      "filename": { "type": "string", "description": "The path to the file. You MUST specify the full path starting with 'user_data/' (e.g. 'user_data/catatan_prompt_al.txt') or absolute '/app/user_data/' path to ensure precise resolution." }
    },
    "required": ["filename"]
  }
}
