node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Do not ignore settings files and config.toml
!**/config.toml

# Ignore local runtime data directory (generated/synced at runtime)
.yuihime

# Ignore generated build manifest (artifact, not source of truth)
src/share/prompts/build-info.json
/build-info.json
bin/yuihime-sea
release/

# opencode session dumps / local scratch debug
session-ses_*.md
tools/tester/
